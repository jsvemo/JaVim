package editor;

import org.junit.Test;
import static org.junit.Assert.*;

public class MacOsTerminalTest {
/*
    @Test
    public void testEnableRawMode() {
        MacOsTerminal terminal = new MacOsTerminal();
        terminal.enableRawMode();
        // Add assertions based on expected behavior
    }

    @Test
    public void testDisableRawMode() {
        MacOsTerminal terminal = new MacOsTerminal();
        terminal.disableRawMode();
        // Add assertions based on expected behavior
    }

    @Test
    public void testGetWindowSize() {
        MacOsTerminal terminal = new MacOsTerminal();
        WindowSize size = terminal.getWindowSize();
        assertNotNull(size);
        assertTrue(size.row() > 0);
        assertTrue(size.col() > 0);
    } */
}